# Glyph Compatibility Notes

Maintain alternate ASCII glyphs for any fancy Unicode characters introduced to the renderer. Update this file whenever glyph mappings change so the compatibility mode remains in sync with the primary art direction.
